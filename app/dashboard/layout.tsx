export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="flex box-border h-screen w-screen bg-black flex-col">
      <div>
        
      </div>
      {children}</div>
  );
}
